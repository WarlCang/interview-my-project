---
description: Mock-interview me about this repo — ingest, interrogate, drill, close the gaps
argument-hint: "[quick|deep|focus <topic>|lang <code>]"
---

Load the `interview-my-project` skill and run the full interview it defines, following
it exactly — it contains every phase, the question-quality bar, the scoring rubric, and
the scorecard format.

Mode from `$ARGUMENTS` (default 8 questions): `quick`, `deep`, `focus <topic>`, or
`lang <code>` to run the interview in another language.

You may delegate Phase 1 (Ingest) to the `decision-scout` subagent to keep transcript
noise out of the interview context.
